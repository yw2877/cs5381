# Shiny Reference 
- Shiny basics: UI + server + shinyApp()
- Common UI: fluidPage(), sidebarLayout(), sidebarPanel(), mainPanel()
- Common outputs: tableOutput() + renderTable(), DTOutput() + renderDT()
- Reactive actions: actionButton() + observeEvent()
- Error handling: validate(), need(), showNotification()